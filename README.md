# Checkout Service

Checkout microservice handling order creation and status.

## Endpoints

- `GET /health` - Health check
- `POST /checkout` - Create checkout order
- `GET /checkout/:orderId` - Get order status

## Run

```bash
npm install
npm start
```
